import { COLORS, SHADOWS, SIZES, FONTS } from './colors';
import IMAGES from './imgs';
import LAYOUT from './layout';
import ROUTES from './routes';

export {
    COLORS,
    IMAGES,
    LAYOUT,
    ROUTES,
    SHADOWS,
    SIZES,
    FONTS
}